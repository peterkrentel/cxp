kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: cxp
nodes:
  - role: control-plane
    extraPortMappings:
      # NATS client port — forward to localhost so dashboard can connect
      - containerPort: 4222
        hostPort: 4222
        protocol: TCP
